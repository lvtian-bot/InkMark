# InkMark 待办

产品出发点、定位与边界见 [product-positioning.md](./product-positioning.md)。

待办记录的任务不代表一定要做，是可以探讨的。如果在实现过程当中会带来更大的问题，或者是说成本大于收益，必须要提出来。

## 记录规则

1. 所有任务必须归入以下五个分类之一：体验与界面、编辑与文件、稳定性与安全、工程与发布、暂不需要。
2. 本次分类迁移完成后，以现有顺序为基准；新增任务一律追加到对应分类的末尾，不插入已有任务之间，不因开始或完成而移动位置。
3. 任务完成后只把 `[ ]` 改为 `[x]`，并在任务末尾按 Obsidian Tasks 格式记录完成时间：`✅ YYYY-MM-DD`。
4. 未完成任务不写完成时间；无法从历史记录确认日期的完成项标记 `✅ 日期待核`。
5. 同一任务只能出现一次。需要调整分类、顺序或新增分类时，先确认再修改。

## 体验与界面

- [x] 状态栏加一个设置按钮；主题调整到视图下面 ✅ 2026-08-08
  - 已实现：状态栏新增设置按钮（⚙）打开设置弹窗，大纲与源码模式入口移到左侧，字数统计保留在右侧；主题统一通过主题菜单和设置弹窗切换。
- [x] 开始页重新设计（v0.0.6） ✅ 2026-08-09
  - 已实现：左品牌栏（InkMark 署名+版本、新建/打开主操作、快捷键提示）+ 右主区最近文件列表；最近文件支持 hover 移除单项与「清除全部」，新增 `recent:remove` / `recent:clear` IPC 与 preload 接口，最近数据模型保持路径字符串数组不变；亮/暗主题走 CSS 变量，空状态有引导。
- [x] 设置面板（v0.0.4） ✅ 2026-08-08
  - 已完成：集中定义设置模型和默认值，统一存储界面主题、内容主题、大纲显示与宽度；旧 localStorage 配置自动迁移。
  - 已提供可扩展的设置对话框，后续新增配置时继续在统一设置模型中扩展。
- [x] 自定义关于窗口（v0.0.4） ✅ 2026-08-08
  - 显示规范的产品名称与当前版本号，并适配亮色/暗色主题。
- [x] 固定格式工具栏（加粗/斜体/标题/列表等，常驻编辑区顶部；支持宽/适中/窄三档宽度） ✅ 2026-08-09
- [x] 启动默认页设置 ✅ 2026-08-09
  - 可选择开始页或空白页；新建标签页始终显示开始页。
- [ ] 所见即所得模式：光标进入段落后，相应的标记浮现，并可编辑；需求与范围见 [live-preview.md](./live-preview.md)（本阶段先做块级，行级后续评估）。
- [x] 标签页可移动 ✅ 2026-08-09
  - 拖拽标签按左右半区判断插入位置，被拖标签半透明、目标位置显示强调色指示线；顺序仅在本次会话有效，不持久化。
- [x] 字体排版有些密集，需探讨确定 ✅ 2026-08-10
  - 已加字体设置（字体族预设 + 字号档位），用户可在设置中切换正文字体与字号，所见即所得与源码模式同步生效。字体预设含系统默认、微软雅黑、思源黑体、等线、宋体、小米 MiSans、鸿蒙 HarmonyOS Sans，未安装的字体自动回落。
  - 设置面板改为左侧分类导航（外观/字体/编辑器/启动）+ 右侧内容，支持后续扩展。
  - 待定：行距/字间距是否需要单独调整；字体设置的实际观感需实测验收。
- [x] 关闭编辑区英文拼写检查红色波浪线 ✅ 2026-08-09
  - 在 Electron 窗口级关闭内置拼写检查，避免技术词、代码与文件路径被大量误判。
- [x] 记住浏览位置，或光标位置，至少切换标签页不能瞎跳。 ✅ 2026-08-10
- [x] 大纲与文件树格式对齐 Obsidian 风格 + 大纲章节折叠 ✅ 2026-08-09
  - 已实现：两侧栏行高亮改为两侧内缩的圆角中性灰、行字号 14、层级缩进 16px/级、面板标题改深色半粗；文件树图标改中性灰、文件名加深；大纲去掉 H1–H6 级别标记，新增章节折叠（父标题小箭头收起/展开，折叠态不持久化、不跨标签串页，活跃标题被折叠时高亮回落到最近可见父级）；折叠可见性与高亮回落为纯函数并有单元测试。
- [x] 关闭最后一个标签页回到欢迎页，而非关闭应用 ✅ 2026-08-10
  - 已实现：移除 `useFile.closeTab` 在最后一个标签页时直接 `closeWindow` 的特殊分支，统一交给 store 处理；store 在标签页清空后自动新建欢迎页标签页（`createTab(undefined, true)`，不受「启动默认页」设置影响，始终回到欢迎页）。窗口仍可通过标题栏关闭按钮或 Alt+F4 关闭。新增 `useStore.test.ts` 回归测试。
- [x] 增加保存按钮 ✅ 2026-08-11
  - 已实现：工具栏末尾新增保存按钮（软盘图标），与 Ctrl+S 同路径（`fileOps.save()`），未保存修改时高亮、保存后恢复；按钮靠右与格式操作分组区分。两种编辑模式共用同一工具栏组件，自动同时生效。
- [x] 增加快捷键设置 ✅ 2026-08-11
  - 已实现：设置面板新增「快捷键」分类，集中管理 10 个应用功能快捷键（新建/打开/打开文件夹/关闭标签/保存/另存为/查找/替换/切换源码/设置）。点击「录入」后按下组合键即捕获（需含 Ctrl/Cmd），Esc 取消；冲突实时标红、有冲突时禁用确定；每行可单独重置，顶部「全部重置」恢复默认。
  - 架构：新增 `src/shared/shortcuts.ts` 统一表示（类型 + 默认映射 + accelerator/格式化/校验/归一化/冲突纯函数），主进程菜单 accelerator 与渲染进程 DOM keydown 都从同一份配置派生；`shortcuts` 作为 `AppSettings` 字段经 localStorage 持久化，新增 `syncShortcuts` IPC 把配置推到主进程重建菜单。旧配置无该字段时自动回落默认。
  - 边界：格式键（加粗/斜体/列表等）沿用编辑器库默认，不在可配置范围；切换源码模式保留 Alt+E 作为固定第二入口。
- [x] 首页最近文件太长，长度需要控制 ✅ 2026-08-11
  - 已实现：「最近打开」栏宽度改为随窗口伸缩（`clamp(320px, 38vw, 560px)`），不再写死；文件名设上限（约 58%）、目录设保底（约 28%），两者都可见且过长时显示省略号，栏宽不再随文字漂移。

## 编辑与文件

- [x] 多标签页 ✅ 2026-08-07
- [x] 源码模式 ✅ 2026-08-07
- [x] 最近文件 ✅ 2026-08-08
- [x] 查找与替换（v0.0.4） ✅ 2026-08-08
  - 已完成：Ctrl+F / Ctrl+H 打开；所见即所得与源码模式均支持上一处、下一处、匹配计数、替换当前和全部替换。
  - 所见即所得模式使用编辑器原生事务，替换可正常撤销并进入文件脏状态；切换标签、模式或内容变化时自动刷新结果。
- [x] Markdown 兼容性清单与回归样例（v0.0.4） ✅ 2026-08-08
  - 已完成：固定样例覆盖标题、段落、引用、链接、图片、代码、表格、普通列表、任务列表、嵌套结构和 GFM 语法。
  - 回归步骤与通过标准见 [markdown-compatibility.md](./markdown-compatibility.md)。
- [x] 图片本地存储（v0.0.4） ✅ 2026-08-08
  - 已完成：粘贴或拖入图片时统一复制到 `${filename}.assets/` 目录，并插入标准 Markdown 相对路径；未保存文档会先提示保存。
  - 支持 PNG、JPEG、GIF、WebP 和 SVG，自动处理安全文件名与重名；非法图片、过大图片和写入失败会显示明确提示。
  - 本地图片通过受控的 `inkmark-local://` 令牌协议显示；切换标签或另存为改变文档路径后会重新解析相对路径。
- [x] 待办任务渲染（GFM task list 复选框 + 点击切换 + 工具栏按钮） ✅ 2026-08-08
- [x] 多文件同时打开（v0.0.4） ✅ 2026-08-08
  - 已实现启动参数解析和打开对话框多选。
- [x] 工具栏按钮补齐（v0.0.4） ✅ 2026-08-08
  - 已完成：代码块、链接、表格三个按钮，WYSIWYG 与源码模式双适配。
- [x] 源码模式升级为 CodeMirror 6（v0.0.5） ✅ 2026-08-08
  - 已完成：语法标记淡化、代码块高亮、查找替换和工具栏适配；引入统一的 `SourceEditorHandle`。
- [x] 文件树工作区（首版：只读浏览） ✅ 2026-08-09
  - 已实现：打开文件夹后以可折叠树浏览目录，点击 `.md` 文件复用既有打开流程切换标签；隐藏文件、`.assets`、`.git` 自动过滤，目录优先按名排序。
  - 工作区根目录通过独立 `WorkspaceWatchManager` 监听，外部新增/删除/重命名后清缓存并刷新已展开目录；不递归监听子目录，深层变化靠下次展开或刷新。
  - 设置面板新增显示开关、左右位置（默认右）与宽度；状态栏、视图菜单和开始页均提供打开文件夹与切换入口；面板支持拖拽调整宽度（左/右两侧方向均正确）。
  - 暂不做：新建/重命名/删除等改写文件系统的操作（`shell:reveal` 已在 IPC 层预留，待后续基础操作补齐时接入）。
- [x] 所见即所得模式下保留原始 Markdown 标记符号 ✅ 2026-08-13
  - 已实现：新增 `plugins/list-marker`，对齐 Milkdown 内置的 strong/emphasis `remarkMarker` 范式（按 mdast `position.start.offset` 回原始串取字符）。四步：① remark 插件回捞 bullet/有序标点；② `extendSchema` 给 bullet\_list/ordered\_list 加标记属性并改解析/序列化 runner；③ 自定义 list handler 优先按节点字符输出（忠实移植 mdast-util-to-markdown，保留 useDifferentMarker）；④ 覆盖无序列表 inputRule（unshift 到 inputRulesCtx 最前），让所见即所得里打「\* /- /+ 」新建的列表也记下字符——内置 inputRule 不传 getAttributes，原本只取默认 '-'。
  - 范围：列表 `-`/`*`/`+` 与有序 `1.`/`1)` 按节点保留（文件加载与打字新建均覆盖）；加粗 `**`/`__`、斜体 `*`/`_` 由 Milkdown 内置 remarkMarker 端到端保留。
  - 回落默认：无序 `-`、有序 `.`、加粗/斜体 `*`，用于工具栏按钮新建、无 position 可回捞的粘贴内容等场景。
  - 测试：`list-marker.test.ts`（15 用例）覆盖检测纯函数与 remark 往返；`list-marker.integration.test.ts`（happy-dom）跑真实 Milkdown parse→serialize 往返，验证「文件加载」与「打字新建」两条路径在 ProseMirror 内核里确实保留 bullet。
- [x] 文件树跟随活动文档 ✅ 2026-08-10
  - 已实现：活动文档路径变化时按规则决定就地展开定位、恢复历史根或切换到文档所在文件夹；方案见 [file-tree-follow.md](./file-tree-follow.md)。
  - 用户显式「打开文件夹」优先于跟随；展开状态按根记录，跨目录切换来回恢复。另存为经路径变化自然触发，无需单独处理。
  - 边缘情况：命令行启动打开多个位于不同文件夹的文件时，初始根为最后打开（当前活动）文件所在文件夹，其余文件夹在会话历史中可切回；同文件夹多文件不受影响。
- [x] 支持frontmatter渲染 ✅ 2026-08-11
  - 已实现：引入 `remark-frontmatter` 解析文档开头的 YAML `---` 块；新建 `frontmatter` atom 节点（YAML 原文存属性）+ NodeView，所见即所得模式下渲染为 Typora 式可编辑文本区（带 `---` 边界、等宽字体、灰底），点进去直接改 YAML、输入即写回节点属性，撤销栈由 ProseMirror 天然支持；序列化时还原为 mdast `yaml` 节点交由 remark-frontmatter 输出。
  - 源码模式原样纯文本；`extractSourceHeadings` 跳过开头 frontmatter 区间，避免结尾 `---` 被误判为 setext 标题；frontmatter 不计入正文字数（WYSIWYG 下 atom 节点天然排除，源码靠 `extractSourceText` 排除区间）。
  - 正文里的 `---`（分隔线 / thematic break）不受影响；查找替换默认不覆盖 frontmatter 块内文本。
- [x] 文件树容易闪烁，保存时闪烁 ✅ 2026-08-11
  - 已实现：根因在主进程 `WorkspaceWatchManager` 把应用自身保存（原子写 temp + rename）触发的目录变更当成外部变更广播。新增 `recordSelfWrite`：保存/另存为时标记文件所在工作区根，开 2 秒抑制窗口，`broadcastChange` 命中未过期记录则跳过广播；复用 shared 的 `isPathInside` 归因路径，仅抑制文件确实落在该根下的情形，正常外部变更不受影响。
  - 渲染端 `useFileTree` 收到事件不再先清空 `dirCache`，改为保留旧缓存直接重载已展开目录（stale-while-revalidate），消除"读取中…"占位闪现的残余闪烁。
- [x] 任务列表渲染方案重构，改为稳定的任务项 NodeView ✅ 2026-08-12
  - 已实现：根因是 `list_item` 合并时 ProseMirror 的 `deleteBarrier`（`before.copy(wrap)`）让结果项继承前一项 `checked`，而 Milkdown 把 Backspace/Delete 都绑到 `liftFirstListItem → joinBackward`，于是非第一项位置按 Backspace 会合并并串台。从源头绕过：任务项行首 Backspace 改走真正的 `liftListItem`（退出列表变普通段落，与空项 Enter 行为一致），不再触发合并；Delete 行首非空任务项沿用 `1bbb69a` 的「删首字符」。
  - 渲染层去掉 `p::before` 伪元素 + 点击坐标判定（`event.clientX > rect.left`）这套 hack：新增 `plugins/task-list-view.ts` 用 ProseMirror widget decoration 在段落行首插入真实 `<span class="task-checkbox">`，点击由全局 handler 反推 `list_item` 位置并 `setNodeMarkup` toggle；CSS 改为针对 `.task-checkbox`。刻意不覆盖 `list_item` 的 NodeView——容器节点的 contentDOM 会破坏 `<li><p>` 结构与现有列表 CSS，widget decoration 是更小、更稳的做法（原计划「NodeView 重构」的稳定渲染目标已达成，实现手段调整为 decoration）。
  - 纯判定函数 `shouldLiftTaskItemOnBackspace` 抽出并单测（7 用例）；`npm run check` 全过。
  - 未覆盖：跨项选区删除、整项 NodeSelection 删除、剪贴板合并等低频路径仍可能串台（appendTransaction 全局纠偏的成本/风险高于收益，未做），如用户反馈再针对性补。
- [x] 无序列表标记显示为空心圆圈，改为实心点 ✅ 2026-08-11
  - 已实现：在 `.theme-inkmark ul` 显式设置 `list-style-type: disc`（实心点）。此前项目样式只设了缩进与行距，列表符号被 Milkdown Nord 主题兜底（Tailwind prose 派系，嵌套列表按 disc→circle→square 轮换），落到空心圆；项目选择器优先级最高，覆盖主题默认，嵌套层级也统一实心点。源码模式与任务列表不受影响。
- [x] 文件对话框记住上次打开位置 ✅ 2026-08-12
  - 已实现：主进程新增独立的 `dialog-state.json` 持久化「上次打开位置」（文件或文件夹），不受最近列表 10 条上限影响；「打开文件」「打开文件夹」对话框均以其为起始位置，打开成功后更新。路径不存在时由系统对话框自动回落。与 `recent-files.json` 职责分开：recent 服务开始页「最近打开」展示，dialog-state 服务对话框定位。
  - 边界：即便频繁打开文件，文件夹定位也不会被挤掉；「另存为」暂未纳入（其对话框预填上次文件名易误覆盖，风险不同）。
- [x] 序列化时去除非用户输入的 `<br />` 空行占位 ✅ 2026-08-13
  - 已实现：根因是 Milkdown `preserveEmptyLine` 特性——paragraph 序列化 runner 对空段落（列表回车产生的空列表项、文档空行）注入 mdast `html` 节点 `<br />` 作占位；序列化方向只走 `remark.stringify`、不运行负责清理的 `remark-preserve-empty-line` 插件（仅解析方向生效），`<br />` 原样落进文件，表现为 `* <br />`。与「保留列表标记」功能无关。
  - 修复：`markdown-stringify-options.ts` 新增 `dropBrPlaceholderHandler`（丢弃值为 `<br />`/`<br>`/`<br/>` 的 html 节点，其余 html 原样），经 `Editor.tsx` 注入 `remarkStringifyOptionsCtx` 的 `handlers.html`。用户手写的 `<br />` 在解析阶段已被删除，故序列化层遇到的必为特性注入，丢弃安全。
  - 排查：穷举序列化方向所有 toMarkdown runner，唯一的非 Markdown 注入即此处；`html.ts` 透传用户 raw HTML、`hardbreak` 为标准硬换行，均非凭空注入；无 `<!---->`/`&nbsp;` 等。
  - 取舍：顺带关闭文档级「空行保留」——所见即所得里多按回车产生的空段落不再用 `<br />` 强行保留，回归标准 Markdown（多余空行折叠为段落分隔，段落不丢），与 Typora/VSCode 一致。
  - 测试：`serialize-br-placeholder.test.ts`（8 用例）覆盖纯函数与真实内核序列化（空列表项/有序列表/嵌套不误伤/往返稳定）。

## 稳定性与安全

- [x] 文件实时监听与 AI 协同编辑（v0.0.4） ✅ 2026-08-08
  - 已实现：持续监听所有已打开文件；干净标签页自动刷新，存在未保存修改时提示选择；区分自身保存与外部写入，并处理重复事件、文件删除或移动及多标签订阅释放。
  - 保留窗口 focus / visibilitychange 时的 mtime 检查作为监听异常时的兜底。
- [x] 文件安全：保存前 mtime 比对 + 冲突提示 + 原子写入（temp + rename） ✅ 2026-08-07
- [x] 冷启动文件竞态修复 ✅ 2026-08-07
- [x] 字数统计 debounce ✅ 2026-08-09
- [x] 冷启动后大纲和字数统计同步 ✅ 2026-08-08
- [x] 文件操作错误反馈 ✅ 2026-08-08
- [x] 外部修改检测延伸 ✅ 2026-08-08
- [x] 内容单一真源与标签页编辑状态隔离 ✅ 2026-08-09
  - 以 `sourceContent` 为内容真源，隔离各标签的编辑状态和撤销历史。
- [x] 源码模式派生状态与导航同步 ✅ 2026-08-09
  - 源码编辑、切换模式和切换标签时同步字数、大纲及定位。
- [x] 外部修改差异审阅与覆盖语义 ✅ 2026-08-09
  - 覆盖外部版本前显示差异，并明确“重载”和“保留”的后果。
- [x] Electron 安全基线（优先度：中） ✅ 2026-08-09
  - 已完成：启用 renderer 与 preload sandbox，保持 context isolation、关闭 Node 集成，并补充 CSP、导航限制、可信渲染进程判断和 IPC 参数校验。

## 工程与发布

- [x] 数据模型收敛：删除 store 顶层镜像字段，改为从 tabs + activeTabId 派生 ✅ 2026-08-07
- [x] EditorState cache 失效：复用 tab 时清除陈旧缓存 ✅ 2026-08-07
- [x] useFile 返回对象 useMemo 稳定化 ✅ 2026-08-07
- [x] ESLint + Prettier 工具链 ✅ 2026-08-09
- [x] 主题模型统一 ✅ 2026-08-09
- [x] 内容安全自动化测试（Vitest） ✅ 2026-08-09
  - 已有 20 个测试，覆盖四类内容安全场景：
    - 标签/模式编辑状态隔离（document-editor-state，3 用例）：按 tab/mode 隔离、陈旧状态不恢复、关闭释放。
    - 外部修改检测与冲突决策（file-conflict-decision，14 用例）：mtime 比较分支、冲突三选一映射、关闭保存/放弃/取消。
    - 冲突差异渲染（conflict-diff，1 用例）：按显示顺序标记增删行。
    - 源码模式大纲与正文提取（source-document，2 用例）：忽略代码块内的标记。
  - 标签增删切换、模式互切的核心保证（编辑状态隔离）由 document-editor-state 覆盖；其数据流转为直白 store 操作，不单独加测，避免引入 DOM 测试基建的技术债。
- [x] 发布质量门禁与工程检查收口 ✅ 2026-08-09
  - 统一 package、lock 和 tag 版本；新增 lint、类型检查、测试、格式检查、构建及分支 CI。
- [x] 明确轻量发布流程与安装包留存要求 ✅ 2026-08-09
  - 新增发布操作说明，区分分支推送与版本标签，并要求自动发布后下载安装包到本地或在本地构建同版本安装包；人工验证按风险保持轻量。
- [x] 项目用户指令与当前工程状态同步 ✅ 2026-08-09
  - 更新测试、质量门禁、默认分支、三进程安全边界、双编辑器架构与专项回归入口；在 AGENTS.md 与 README.md 中明确用户与 AI Agent 的开发分工、重大技术路线确认机制和具体实现责任，删除已过时和重复的说明。
- [x] 完善 README 对外说明与使用入口 ✅ 2026-08-09
  - 补充核心功能、Windows 下载与安装、基本使用、产品边界、质量检查、问题反馈和项目文档入口，并按普通用户优先的顺序重组内容。
- [x] 发布 v0.0.6 ✅ 2026-08-09
  - 收录字体与字号设置、设置面板分栏、Markdown 固定标记风格、拼写检查关闭、项目文档整理和发布前类型清理；完整质量门禁与 Windows NSIS 打包通过。
- [x] 发布 v0.0.7 ✅ 2026-08-10
  - 收录文件树工作区（只读浏览与外部刷新）、侧栏格式对齐与大纲章节折叠、文件树跟随活动文档等；完整质量门禁通过并推送到 GitHub Release。
- [x] 发布 v0.0.9 ✅ 2026-08-12
  - 收录快捷键设置（录制式录入、冲突检测、菜单与 keydown 统一从配置派生）、frontmatter 渲染、保存按钮、文件树保存闪烁修复、无序列表标记改实心点、首页最近文件限长；完整质量门禁与 Release 工作流通过，产物与本地安装包留存完整。

## 暂不需要

- [ ] 导出 HTML / PDF / 富文本
- [ ] 版本快照
- [ ] 自动更新
- [ ] 专注模式
- [ ] 可选自动保存（默认关闭）
- [ ] 数学公式与图表（KaTeX / Mermaid）
- [ ] 外部改动审阅（编辑区内联 diff，逐块接受/拒绝）；需求见 [change-review.md](./change-review.md)
