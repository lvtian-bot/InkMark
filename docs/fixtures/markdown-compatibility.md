# InkMark Markdown 兼容性样例

这是一段普通段落。它包含 **粗体**、_斜体_、_**粗斜体**_、~~删除线~~、`行内代码` 和被转义的 \*星号\*。

这一行末尾有反斜杠，用于检查显式换行。\
这一行应紧跟在上一行下方。

这两行之间只有一个换行符，用于检查软换行，
第二行仍属于同一段落。

## 二级标题

### 三级标题

#### 四级标题

##### 五级标题

###### 六级标题

---

## 引用

> 一级引用包含 **强调文字**。
>
> - 引用内列表一
> - 引用内列表二
>
> > 二级引用内容。

## 链接与图片

[InkMark 仓库](https://github.com/lvtian-bot/InkMark 'InkMark')

<https://example.com>

![InkMark 回归图片](./markdown-regression-image.svg '本地相对路径图片')

## 代码

行内代码中的 Markdown 标记不应生效：`**not bold**`。

```typescript
function greet(name: string): string {
  return `Hello, ${name}`;
}
```

## 普通列表

- 无序项目一
- 无序项目二
  - 嵌套项目二点一
  - 嵌套项目二点二
    1. 更深一层的有序项目
    2. 更深一层的第二项
- 无序项目三

1. 有序项目一
2. 有序项目二
   1. 嵌套有序项目
   2. 嵌套有序项目二
3. 有序项目三

## 任务列表

- [ ] 未完成任务
- [x] 已完成任务
- [ ] 带嵌套项的任务
  - [ ] 嵌套未完成任务
  - [x] 嵌套已完成任务

## 表格

| 左对齐      |     居中      | 右对齐 |
| :---------- | :-----------: | -----: |
| 单元格 A    |   单元格 B    |    100 |
| 包含 `代码` | 包含 **粗体** |    200 |

## 列表内代码块

1. 代码块前的项目

   ```text
   list item code
   second line
   ```

2. 代码块后的项目

## 回归操作区

执行下列操作后保存并重新打开：

- 将“待修改文本”替换为“已修改文本”：待修改文本。
- 把下面的未完成任务切换为已完成。
- 在下面的列表中新增“新增项目”。
- 在下面的表格末尾新增一行“新增 | 行”。

- [ ] 待切换任务

- 原有项目

| 操作 | 结果 |
| ---- | ---- |
| 原有 | 行   |
